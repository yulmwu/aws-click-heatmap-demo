{
    "name": "heatmap-click-producer",
    "private": true,
    "version": "0.1.0",
    "type": "module",
    "scripts": {
        "start": "vite",
        "build": "tsc -b && vite build",
        "preview": "vite preview"
    },
    "dependencies": {
        "@aws-sdk/client-kinesis": "^3.600.0"
    },
    "devDependencies": {
        "typescript": "^5.5.4",
        "vite": "^5.4.0"
    }
}
